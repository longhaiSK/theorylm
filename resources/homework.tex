\usepackage{xcolor}
\usepackage[most]{tcolorbox}

% Define the specific CSS colors
\definecolor{solborder}{HTML}{d9534f}
\definecolor{solbg}{HTML}{f9f2f4}

% Define the 'sol' environment
\newtcolorbox{sol}{
  colback=solbg,          % Background color
  colframe=solborder,     % Border/Title color
  coltitle=white,         % Title text color
  title=\textbf{Solution},% The content of the header
  fonttitle=\bfseries,    % Bold title font
  boxrule=1.5pt,          % Approx 2px border
  arc=5pt,                % Rounded corners (border-radius)
  breakable,              % Allow splitting across pages
  parbox=false,           % Allow normal paragraph breaks inside
  before skip=1em,        % Margin top (approx)
  after skip=1em          % Margin bottom (approx)
}


